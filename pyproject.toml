[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-neural-kernel"
version = "0.1.0"
description = "Execution trace to training pair converter for neural Plato"
license = {text = "MIT"}
requires-python = ">=3.8"
[project.urls]
Repository = "https://github.com/SuperInstance/plato-neural-kernel"
